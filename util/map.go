package util

type UniqueStringMap map[string]interface{}

func (m UniqueStringMap) Keys() []string {
	keys := make([]string, len(m))
	i := 0
	for k := range m {
		keys[i] = k
		i++
	}
	return keys
}
